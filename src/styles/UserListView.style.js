import { StyleSheet } from "react-native";

export default StyleSheet.create({

  header: {
    flexDirection: "row",
    justifyContent: "flex-end",
    height: 30,
    alignItems: "center",
    borderBottomWidth: 1
  },

  headerSmallText: {
    fontSize: 20
  }

});